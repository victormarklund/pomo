# pomo

A simple CLI pomodoro timer.

Currently depends on "libnotify" to send notifications.

Example:

```sh
pomo -x 3 -f 25 -b 5
```

Runs a configuration of 3 blocks with 25 minutes of focus time and a 5 minute break.
